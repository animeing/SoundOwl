{
  "installCommand": "npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "js",
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "server/**",
          "css/**",
          "img/**",
          "js/**",
          "parts/setting.ini",
          "manifest.json",
          "favicon.ico",
          "favicon.svg",
          "favicon_*.png"
        ]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.js"
    }
  ]
}
